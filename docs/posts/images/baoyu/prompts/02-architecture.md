Create a professional infographic following these specifications.

## Image Specifications
- Type: Infographic (diagram)
- Layout: linear-progression (vertical process flow, 3 nodes top→bottom with arrows)
- Style: corporate-memphis (flat vector, vibrant geometric fills, NO outlines, solid fills)
- Aspect Ratio: 3:4 (portrait)
- Language: Simplified Chinese
- Background: LIGHT — white / soft pastel. Must be a light-background design, not dark.

## Core Principles
- Clean, generous whitespace; strong visual hierarchy; modern, professional.
- Render ALL Chinese text and the code identifiers EXACTLY (strategy.py, backtest.py, Sortino), correct characters, no garbled glyphs.
- Calm finance/tech palette on white: indigo, teal, amber accents. Three node cards in distinct accent colors.

## Layout Guidelines (vertical linear-progression)
- Title at top.
- Three rounded node cards stacked vertically, connected by downward arrows.
- Node 1 emphasized as "editable" (warm/amber), Node 2 as "fixed/locked" (indigo, with a small lock motif), Node 3 as the "score" outcome (teal, with a small trophy or flag motif). IMPORTANT: do NOT draw any numeric value, gauge reading, dial number, or decimal anywhere on Node 3 — show only the text, no numbers.
- Below the flow: two short "hard rule" chips side by side, numbered 1 and 2.

## Content (preserve faithfully)

TITLE:
把量化研究压成：一个策略 + 一个打分器

KICKER (small): 固定评估器 + 单一可编辑程序

NODE 1 (amber card, badge text 「改它」):
strategy.py
desc: 唯一可编辑程序 —— 提假设、改逻辑、调参数，只动这一个文件

ARROW DOWN, small label: 只读它，绝不为提分而改

NODE 2 (indigo card with small lock icon, badge text 「钉死」):
backtest.py
desc: 固定评估器 —— 拉数据 · 模拟成交 · 计成本 · 算指标

ARROW DOWN

NODE 3 (teal card):
一个分数 ＝ Sortino
desc: 同数据 · 同口径 · 同公式 → “有没有更好”成为可排序的客观问题

TWO HARD-RULE CHIPS (numbered):
1. 只改策略文件，评估器 / 数据 / 评分公式一律不动
2. 每轮只动一个有经济含义的变量，严格更好才留

Text labels (Simplified Chinese): 把量化研究压成：一个策略 + 一个打分器；固定评估器 + 单一可编辑程序；strategy.py；改它；唯一可编辑程序；backtest.py；钉死；固定评估器；一个分数 ＝ Sortino；可排序的客观问题；只改策略文件，评估器/数据/评分公式不动；每轮只动一个有经济含义的变量，严格更好才留
